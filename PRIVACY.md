# Rule-Bot 隐私说明

本说明覆盖 Telegram 私聊、受信任群聊，以及可选的 Rule-Bot Client 私用和社区入口。自行部署者负责选择 Telegram、GitHub、DNS、CDN、反向代理和日志存储服务，并向实际用户说明相应的数据处理边界。

## Telegram 使用

Rule-Bot 接收 Telegram 提供的消息文本、聊天信息和用户身份，用于解析域名、维持当前交互状态、执行限流和生成提交身份。私聊状态和待确认操作仅保存在进程内存中，超时后清理；Rule-Bot 不建立 Telegram 消息历史数据库。

启用群聊模式后，Rule-Bot 只处理已加入 `ALLOWED_GROUP_IDS` 且明确提及机器人的文本消息。业务日志不记录原始消息、原始待检查域名、Telegram 用户 ID 或用户名；处理事件使用每次进程启动时随机生成密钥计算的短引用。启动配置和群组播报日志仍可能包含部署者配置的目标群组 ID。

Telegram 用户成功添加规则时，Rule-Bot 会把界面中展示的提交者身份、规则、可选说明和提交时间写入公开 GitHub 提交。提交前的确认页面会显示这些公开字段。群聊模式和管理员强制添加不会再次执行私聊确认，因此只应在受信任群组中启用。

## Rule-Bot Client 入口

Rule-Bot Client API 只接受 `version` 和 `domain`。服务端不接受客户端指定仓库、规则文件、提交身份、说明或强制添加参数。

新版 Rule-Bot Client 默认在本地执行以下处理：

- 将完整子域名缩减为公共后缀规则下的可注册域名；
- 对同一可注册域名只投递一次；
- 在本地排除 `.cn` 和用户配置的排除项；
- 不发送 URL 路径、查询参数、网页内容、来源应用、访问次数或时间戳。

第三方客户端可以采用不同策略。Rule-Bot 只验证收到的单个域名字段，不能保证第三方客户端已经执行相同的本地缩减。

## 社区 Token 与账号关联

社区 Token 必须由用户在 Telegram 私聊中阅读本说明并主动同意后申请。服务端数据库保存 Telegram 用户 ID、随机 Token 主体、签发时间、到期时间、启用状态和隐私说明版本，用于群成员验证、限流、重新签发、过期、吊销和撤回同意。

Token 自身只包含随机主体，不包含 Telegram 用户 ID。API 鉴权、请求限流和自动规则提交使用随机主体，不把 Telegram 身份写入规则。撤回同意会吊销当前 Token；隐私说明版本更新后，用户需要重新确认。

## 网络和外部服务

Rule-Bot 会把待检查域名交给配置的 DNS over HTTPS 服务，并使用 GeoIP、GeoSite 和目标 GitHub 仓库完成规则判断。相应服务提供方可能处理域名、出口 IP、请求时间和常规网络元数据。

公网入口经过 Cloudflare、反向代理或其他 CDN 时，这些服务也会处理连接元数据。Rule-Bot 关闭 Client API 的 HTTP access log，业务代码不读取客户端 IP。客户端可配置可信代理改变入口看到的出口 IP，但代理提供方会看到连接目标和时间。

## 日志、保留和公开范围

- 被拒绝、无效或重复的域名不写入 Rule-Bot 数据库；业务日志使用短引用，不记录原始域名。
- Docker Compose 示例把日志限制为两个 1 MiB 文件；其他部署方式由部署者设置日志保留策略。
- 社区 Token、隐私同意和吊销状态保留在配置的数据库中，直到部署者清理或用户撤回同意。
- 成功添加的域名、提交者身份、可选说明和提交时间会公开写入目标 GitHub 仓库及提交历史，不能作为临时私密数据撤回。

## 用户选择

不接受上述边界时，不要通过 Telegram 提交规则，也不要启用 Rule-Bot Client 投递。社区用户可以在机器人私聊中吊销 Token 或撤回同意。Rule-Bot Client 用户还可以在本地排除敏感后缀、关闭 `rule_bot.enabled`，或只保留本地收集。
