target-version = "py314"

[lint]
# Keep the project's long-standing Ruff contract explicit so a Ruff release
# cannot silently enable additional rule families for unrelated dependency PRs.
select = ["E4", "E7", "E9", "F"]
